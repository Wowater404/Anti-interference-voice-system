# 声纹训练流水线使用说明（质量门控版）

> 目标：**丢原始音频 → 一键出模型**，中间自动完成 增强 → 预处理 → **脏样本过滤** → 划分 → 训练。
> 新增：`quality_gate.py`（脏样本检测）、`run_train_pipeline.py`（主控）、`prepare_processed_train_data.py`（补丁：记录 sim_abs / sep_best_sim）。

---

## 一、完整流程

```
原始 datasetA
  ↓ ① augment_dataset.py        每样本 8 版本增强（音量/变调/加噪/裁剪）
增强数据 datasetA_aug
  ↓ ② prepare_processed_train_data.py
        kws: 降噪 + 自适应盲分离（与推理 _process_kws 一致）
        cmd: Renoise 降噪 + 自适应 SpEx+ 分离（sim<0.28 才触发）
        ★ 新增：记录 sim_abs（分离前）、sep_best_sim（分离后）
处理后数据 datasetA_aug_processed
  ↓ ③ quality_gate.py           ★ 核心新增：脏样本检测 + 过滤
过滤后数据 → gated jsonl
  ↓ ④ make_folds.py             按 orig_id 5折划分（防泄漏）
folds/fold_{0..4}/
  ↓ ⑤ train_camplus / train_eres2netv2
声纹模型权重
```

---

## 二、质量门控检测什么（为什么需要它）

训练数据"经过处理"≠"干净"。质量门控用**可量化的 sim 指标**抓三类脏样本：

| 规则 | 检测目标 | 判定条件 | 默认阈值 |
|---|---|---|---|
| **G1** neg_high_sim | neg 实际是同一个人（标注错误）/ 锚点错误 | label=0 且 `sim_abs > 0.55` | 0.55 |
| **G2** pos_low_sim | pos 目标声纹提取失败（分离选错/锚点脏） | label=1 且 `sep_best_sim < 0.30` | 0.30 |
| **G3** jump_artifact | 分离伪影（分离后 sim 跳变异常） | 触发分离且 `sep_best_sim - sim_abs > 0.30` | 0.30 |
| G4（仅报告） | 分离无提升 | 触发分离但无改善 | — |

> **G1/G2 是"语义脏"**（处理救不了，只能剔除）；**G3 是"失真脏"**（处理制造的问题）。

---

## 三、快速开始

### 场景 1：全流程（含增强，从原始数据开始）

```bash
cd F:/龙虾/2026-07-18-13-57-00/voice_pipeline
python tools/run_train_pipeline.py \
  --src "C:/Users/善水/Desktop/datasetA/datasetA" \
  --work_root "F:/龙虾/2026-07-18-13-57-00/datasetA_aug" \
  --processed_root "F:/龙虾/2026-07-18-13-57-00/datasetA_aug_processed" \
  --train_model both --fold full --epochs 10 --batch 64
```

### 场景 2：已有增强数据，只做 ②③④⑤（推荐日常用）

```bash
python tools/run_train_pipeline.py \
  --work_root "F:/龙虾/2026-07-18-13-57-00/datasetA_aug" \
  --processed_root "F:/龙虾/2026-07-18-13-57-00/datasetA_aug_processed" \
  --skip_augment --train_model both --fold full
```

### 场景 3：先只出质量门控报告，不定过滤（定阈值用）

```bash
python tools/run_train_pipeline.py \
  --work_root ... --processed_root ... \
  --skip_augment --skip_train --no_gate_filter
# 然后看: gated/quality_report.json + gated/quality_sim_dist.csv
```

---

## 四、首次使用：必须先人工定阈值（一次性的）

质量门控的过滤阈值（G1/G2/G3）**不能拍脑袋定**，首次使用流程：

1. **跑场景 3** → 生成 `quality_report.json` + `quality_sim_dist.csv`
2. **看 CSV**：pos/neg 的 `sim_abs`、`sep_best_sim` 分布（用 Excel/脚本排序）
3. **抽查 20-30 条边界样本**：
   - neg 里 `sim_abs > 0.5` 的：真的是同一个人吗？（听 kws 和 cmd）
   - pos 里 `sep_best_sim < 0.35` 的：目标声纹提取失败了吗？
4. **调整阈值**后跑正式流程：
   ```bash
   python tools/run_train_pipeline.py ... --no_gate_filter 不加了
   # 直接改 quality_gate.py 里的默认阈值，或用 --neg_high_sim 等参数
   ```

> ⚠️ 如果发现大量样本 `sim_abs` / `sep_best_sim` 字段**缺失**（report 里 `missing_gate_fields` 很大），说明用的是旧版预处理结果——**需要重跑 ②**（新版才记录这两个字段）。

---

## 五、断点续跑

每阶段完成会生成 `.stage_*.done` 标记文件：

| 标记 | 位置 | 含义 |
|---|---|---|
| `.stage_augment.done` | work_root | 增强已完成 |
| `.stage_prepare.done` | processed_root | 预处理已完成 |
| `.stage_folds.done` | folds 数据源 | 划分已完成 |

重跑同一命令会自动跳过已完成阶段。**想强制重跑某阶段**：删掉对应标记文件，或用 `--skip_*` 跳过不要的阶段。

---

## 六、质量门控如何影响训练数据（重要）

- 默认（不指定 `--no_gate_filter`）：门控过滤后的数据会**覆盖** `pos_aug_processed.jsonl` / `neg_aug_processed.jsonl`，`make_folds` 和训练**只吃到过滤后的干净数据**。
- 指定 `--no_gate_filter` 或 `--skip_gate`：folds 用**未过滤**数据（用于对照实验）。

**建议做 A/B 对比**：同一 fold 下，分别用"过滤前"和"过滤后"训练，对比 val EER——用数据证明门控的收益。

---

## 七、参数速查

| 参数 | 作用 | 默认 |
|---|---|---|
| `--src` | 原始 datasetA 目录（跑增强时需要） | None |
| `--work_root` | 增强数据根目录 | 必填 |
| `--processed_root` | 处理后数据目录 | 必填 |
| `--gate_root` | 门控输出目录 | processed_root/gated |
| `--skip_augment/prepare/gate/train` | 跳过各阶段 | False |
| `--no_gate_filter` | 门控只报告不过滤 | False |
| `--train_model` | camplus / eres2netv2 / both | both |
| `--fold` | 0-4 或 full | 0 |
| `--epochs / --lr / --batch / --workers` | 训练超参 | 10 / 1e-4 / 64 / 4 |
| `--config` | pipeline config | default.yaml |

---

## 八、与推理侧的一致性保证

- ② 预处理复用 `pipeline._process_kws` 和 cmd 降噪+分离逻辑，与推理完全一致
- 分离触发条件、跳变上限、声纹选轨均与 `pipeline.py` 相同
- 新增的 `sim_abs` / `sep_best_sim` 字段只影响训练侧质量判断，不影响推理

---

## 九、注意事项

1. **训练/推理降噪模型必须一致**：`default.yaml` 是 GTCRN，README 说 V8 用 Renoise（`verify_dual_full.yaml`）。跑流程前确认 `--config` 指向实际使用的配置。
2. **从预训练重训**：训练脚本已强制 `init_from=None`，换数据后不能继承旧权重。
3. **两模型权重独立**：camplus 和 eres2netv2 可在两台电脑并行训练。
4. **阈值经验值**：G1=0.55、G2=0.30、G3=0.30 是初始建议，**务必按你数据分布调整**。
