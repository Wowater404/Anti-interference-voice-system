# 团队模块集成规范（Voice Pipeline）V2

> 适用仓库：Anti-interference-voice-system / voice_pipeline
> 制定日期：2026-07-27 ｜ V2 更新：2026-08-06
> 目标：多小组各自更换负责模块，最终三模块**共存于 main**；仓库保持**纯推理状态**，训练（微调）由整合者最后统一进行

---

## 0. 集成意图与分工边界（V2 明确）

各小组**只改自己负责的 `.py` 模块**，确认更新模型后合并，最终三个小组的更新模型**全部共存于 `main`**。

**关键分工边界**：

| 角色 | 职责 |
|------|------|
| **组员** | 更换/新增自己负责的模块代码，保证**预训练冻结态**下接口通、能跑通 |
| **组员不负责** | ❌ 训练/微调——**不是团队协作工作**。所有训练推迟到模型全部选定、架构锁定后，由**整合者**统一进行 |
| **整合者** | 合并后统一 config、端到端终评、**最后一环统一微调**（用本地保留的脚本 + 增强数据，不要求组员参与） |
| **仓库定位** | **纯推理流水线**：只含推理代码 + 官方预训练权重配置，不含任何训练脚本/微调产物 |

当前 `main` 模块接口契约（替换模型必须保持，否则流水线无法接线）：

| 模块 | 文件 | 输入 → 输出 |
|------|------|-------------|
| 降噪 | `modules/denoiser.py` | `[N]float32[-1,1] → [N]float32` |
| 人声分离 | `modules/separator.py` | `[N] → ([N], list[[N]]) + target_embedding 选轨` |
| 声纹 | `modules/voiceprint.py` | `[N]float32 → [192]float32(归一化)` |
| ASR | `modules/asr.py` | `[N]float32 → str` |

---

## 1. 分支纪律（对组员，四条强制）

1. **从最新 `main` 分出**
   - 开工前 `git checkout main && git pull` → `git checkout -b <你的分支>`。
   - 禁止"本地另起炉灶、与 main 无共同祖先"的孤儿提交。

2. **只改自己负责的 `.py` + 通过选择器注册**
   - 新增模型时，在对应 `modules/*.py` 里加一个实现类，并在 `configs/default.yaml` 的**选择器字段**加一个新选项值。
   - **不要**改写别人模块的字段值（见 §3）。

3. **预训练冻结态自测（组员不做训练）**
   - 开发/提交阶段一律用**官方预训练（冻结）模型**：`finetuned_path` 可为 `null`、阈值用预训练默认。
   - **组员不需要做任何训练/微调**——训练由整合者在最后阶段统一完成，不在组员任务范围内。
   - **调参 ≠ 训练**：组员可自由调整**配置参数**（threshold、分离阈值、降噪强度、路由策略等，改 `configs/default.yaml` 数值即可），在冻结模型上推理验证效果；但**不得执行训练**（更新模型权重）。注意：冻结态扫出的阈值在微调后需由整合者重新扫描。

4. **不删除仓库文件**
   - 只允许**新增/修改**自己负责的模块文件。
   - 删除任何文件（含工具脚本、历史记录）必须在 PR 中说明并获团队确认；删除推理必需文件视为违规。

---

## 2. 模块负责人与对应文件

| 负责小组 | 模块 | 负责文件 |
|----------|------|----------|
| 组 A | 降噪 | `modules/denoiser.py` |
| 组 B | 人声分离 | `modules/separator.py` |
| 组 C | 声纹 + ASR | `modules/voiceprint.py` + `modules/asr.py` |

> 各组之间没有代码重叠（各管一个 `.py`），可直接共存——前提是 §1 纪律被遵守。

---

## 3. 冲突规避：config 选择器机制

所有模型切换通过 `configs/default.yaml` 的**选择器字段**完成，互不覆盖：

```yaml
denoise:
  model: gtcrn          # 组A可改为其他（仅加选项，不改别人）
  # gtcrn: { ... }      # 组A新增的参数块

separation:
  enable: true
  model: spex_plus      # 组B可改为其他（仅加选项）
  # 分离阈值字段由组B维护

voiceprint:
  model: cam_plus       # 组C可改为 ensemble 等（仅加选项）
  cam_plus:
    finetuned_path: null   # 开发阶段冻结；微调由整合者最后统一处理
  threshold: <预训练默认>

asr:
  model: paraformer     # 组C可改为其他（仅加选项）
```

**合并时**：最终 `main` 的 config = 各组只动自己那一行的选择器值，其他行保持默认，三分支互补而非互斥。

---

## 4. 合并流程

1. 各组完成自己模块替换 + **预训练冻结态** fold_0 自测（仅验证"接口通、能跑通"）。
2. 各自从最新 `main` 开 PR（禁止孤儿分支）。
3. 合并顺序建议：先合"只加选项不改默认值"的分支，最后合声纹/ASR 分支。
4. 全部合入后，由整合者出一份**统一 config**，在 fold_0 验证折做**端到端终评**，确认各新模块叠加后无退化。
5. **统一微调（整合者工作，不要求组员）**：架构锁定后，用**本地保留**的训练脚本 + 增强 datasetA 统一微调声纹；若期间改了降噪，必须按新降噪输出重训（重训泡原则）；按需微调 ASR。微调产物按需入库。

---

## 5. 仓库纪律（V2 新增，强制）

1. **纯推理仓库**：`main` 只含推理必需代码 + 官方预训练权重配置；**不含训练/分析脚本、微调产物**（训练脚本与增强数据本地保留，不入库）。
2. **新模型权重**：引入需要额外权重的模型（ONNX/checkpoint 等），必须随 PR 提供下载方案或直接入库；仓库缺失则合入前需补齐。
3. **删除需确认**：删除任何被跟踪文件需在 PR 中说明。
4. **main 保护**：`main` 受分支保护，一律通过 PR 合并。

---

## 6. 一句话总结

> 组员：只改自己的 `.py` + 选择器 + 预训练冻结局测能跑 = 提交；仓库：纯推理、权重有来源 = 干净；微调：整合者最后一环统一做 = 不要求组员。三模块干净共存。
