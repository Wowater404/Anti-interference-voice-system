# 权重分发指南（owner 操作手册）

本文件说明如何把微调声纹权重从 Git LFS 迁移到「HuggingFace + GitHub Releases」双镜像，
让组员 clone 后只需 `python tools/download_finetuned.py` 即可获取权重，不再依赖 git-lfs。

---

## 一、已经帮你完成的改动

| 文件 | 改动 |
|------|------|
| `tools/download_finetuned.py` | 新增：双源下载脚本（HF 优先，GitHub 回退），内置 SHA256 校验 |
| `.gitignore` | 新增 `finetuned_models/` 忽略规则 |
| `.gitattributes` | 删除 `*.pt` 的 LFS 通配规则（不再一刀切 LFS） |
| `README.md` | 「快速开始」「权重保存位置」等 4 处由 `git lfs pull` 改为下载脚本 |
| Git 索引 | 已 `git rm --cached` 暂存 5 个权重（本地文件保留） |

> 说明：`finetuned_models/` 下 5 个权重文件（合计约 228MB）已从 Git 索引移除但本地仍在，
> 尚未 commit/push。下面的操作完成后组员才能看到效果。

---

## 二、上传权重到两个镜像（二选一即可，建议都做）

### 方式 A：GitHub Releases（网页操作，最简单）

1. 打开：<https://github.com/Wowater404/Anti-interference-voice-system/releases/new>
2. `Tag` 填 `v8-weights`（与脚本里 `GH_TAG` 一致）
3. 上传这 5 个文件（保持原文件名）：
   - `camplus_v7_full.pt`
   - `eres2netv2_v7_full.pt`
   - `camplus_v6_fold0.pt`
   - `camplus_v7_fold0.pt`
   - `eres2netv2_v7_fold0.pt`
4. 点击 `Publish release`

> 命令行替代（需已安装 gh 并登录）：
> ```bash
> gh release create v8-weights \
>   finetuned_models/camplus_v7_full.pt \
>   finetuned_models/eres2netv2_v7_full.pt \
>   finetuned_models/camplus_v6_fold0.pt \
>   finetuned_models/camplus_v7_fold0.pt \
>   finetuned_models/eres2netv2_v7_fold0.pt
> ```

### 方式 B：Hugging Face Hub（国内访问通常更快）

1. 在 <https://huggingface.co> 创建一个 **model 仓库**，id 建议为
   `Wowater404/anti-interference-voice-weights`（与脚本里 `HF_REPO_ID` 一致；若改名字，同步改脚本顶部常量）。

2. 上传（二选一）：

   **命令行（推荐）**：
   ```bash
   pip install huggingface_hub
   huggingface-cli login   # 粘贴你的 HF token
   huggingface-cli upload Wowater404/anti-interference-voice-weights \
     finetuned_models/camplus_v7_full.pt \
     finetuned_models/eres2netv2_v7_full.pt \
     finetuned_models/camplus_v6_fold0.pt \
     finetuned_models/camplus_v7_fold0.pt \
     finetuned_models/eres2netv2_v7_fold0.pt \
     . --repo-type model
   ```

   **网页上传**：进入仓库 → `Files` → `Upload files`，拖入 5 个文件。

3. 确认文件 URL 形如：
   `https://huggingface.co/Wowater404/anti-interference-voice-weights/resolve/main/camplus_v7_full.pt`

---

## 三、提交并推送代码

上传完成后，提交本地改动并推送（把权重从远程仓库移除）：

```bash
cd F:/龙虾/2026-07-18-13-57-00/voice_pipeline
git add .gitignore .gitattributes README.md tools/download_finetuned.py
git add -u finetuned_models/   # 暂存权重的删除
git commit -m "chore: 权重分发改为 HF/GitHub Releases 双镜像，移除 Git LFS 依赖"
git push
```

> 若远程仍是 `release/v8-dual-full` 分支，push 到该分支；组员 clone 默认分支后即可用脚本下载。

---

## 四、验证组员流程

组员侧现在只需：

```bash
git clone https://github.com/Wowater404/Anti-interference-voice-system.git
cd voice_pipeline
python tools/download_finetuned.py        # 自动从 HF/GitHub 下载 2 个生产权重
python tools/download_finetuned.py --all  # 需要交叉验证权重时再加
```

下载脚本会用 SHA256 校验文件完整性，与 `download_spexplus.py` 同一套机制。

---

## 五、可选的后续清理

- `pretrained/spex_plus/checkpoint.pth` 同样还在 LFS 里（约 2.6MB），已有 `download_spexplus.py` 管理，
  可同理 untrack：删除 `.gitattributes` 中该行 + `git rm --cached` + `.gitignore` 忽略 `pretrained/spex_plus/`。
- `runs/` 目录已被 `.gitignore` 忽略，训练中间 checkpoint 不会进仓库，可安心本地保留或删除。
