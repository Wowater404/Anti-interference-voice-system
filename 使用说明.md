# 抗干扰语音指令识别系统 — 队员调试使用说明

> 挑战杯 XH-202615 | 美的集团发榜 | 当前版本: V8 双微调融合 (2026-08-09)

## 一、本仓库包含什么

```
voice_pipeline/
├── run_inference.py          # 主推理入口 (含峰值内存统计)
├── config.py                 # 配置解析
├── pipeline.py               # 流水线组装 (降噪→分离→声纹→ASR)
├── modules/
│   ├── denoiser.py           # 降噪工厂 (支持 noisereduce/renoise/gtcrn)
│   ├── renoise.py            # 频谱门限降噪 (最终方案, 4参数可调)
│   ├── separator.py          # SpEx+ 说话人分离 (自适应触发)
│   ├── voiceprint.py         # 声纹鉴别 (CAM++/ERes2NetV2/ResNetSE/Ensemble)
│   └── asr.py                # ASR (Paraformer, 去标点)
├── configs/
│   ├── verify_dual_full.yaml # ★最终配置: 双 fold_full 微调融合
│   ├── verify_dual_finetune.yaml  # 双 fold0 微调融合 (验证折无偏 60.91)
│   └── ...
├── finetuned_models/         # 微调模型权重 (Git LFS)
│   ├── camplus_v7_full.pt    # CAM++ v7 fold_full (28MB)
│   ├── eres2netv2_v7_full.pt # ERes2NetV2 v7 fold_full (71MB)
│   └── ...
├── results/
│   └── submission_datasetA_dual.json  # datasetA 提交文件 (官方格式)
└── tools/                    # 训练/评估/打包脚本
```

## 二、环境要求

```bash
# Python 3.10+ / 3.12 均可
pip install torch torchaudio modelscope funasr noisereduce editdistance
pip install soundfile librosa numpy scipy
# 说话人分离 (SpEx+)
pip install speechbrain
# Git LFS (拉取模型权重用)
git lfs install
```

- GPU: 建议 6GB+ 显存 (RTX 3060/4050+), 官方测试环境 L20/3090 均可
- 模型 (CAM++/ERes2NetV2/Paraformer) 首次运行从 ModelScope 自动下载

## 三、快速开始

### 1. 拉取模型权重 (Git LFS)

```bash
git lfs pull
# 或 clone 时自动拉取: git clone <repo-url> && cd voice_pipeline && git lfs pull
```

### 2. 推理 (单条音频)

```bash
python run_inference.py --kws 唤醒音频.wav --cmd 识别音频.wav
# 输出: 识别文本 (非目标说话人输出空字符串)
```

### 3. 推理 (dataset 批量, 比赛格式)

```bash
# data_root 需含 pos.jsonl + neg.jsonl (与 datasetA 格式一致)
python run_inference.py --config configs/verify_dual_full.yaml \
    --data_root F:/挑杯资料/datasetA \
    --split all \
    --output results/result.json
# 输出: 比赛提交格式 JSON (含 avg_cer/avg_rr/duration, 官方CER口径)
```

### 4. 验证折无偏评估 (预测比赛成绩参考)

```bash
python run_inference.py --config configs/verify_dual_finetune.yaml \
    --data_root <fold0验证折目录> \
    --split all --output results/val_fold0.json
# 参考: 双fold0融合 = 60.91分 (模型未见过该数据, 最诚实)
```

## 四、当前最优配置 (verify_dual_full.yaml)

| 模块 | 配置 |
|---|---|
| 降噪 | Renoise 频谱门限 (stationary=False, prop=0.8, nstd=1.5, nfft=1024) |
| 分离 | SpEx+ 16kHz 自适应 (仅低置信样本触发) |
| 声纹 | CAM++v7(fold_full) + ERes2NetV2v7(fold_full) + ResNetSE原版, 权重 0.4/0.4/0.2, zscore阈值 -0.17 |
| ASR | Paraformer (输出去标点) |

**最终成绩 (datasetA 全量 1838 条)**: CER 0.3793 | RR 0.9958 | 80分 64.66 | 推理 1427s | 峰值内存 7.14GB

## 五、常见问题

| 问题 | 解决 |
|---|---|
| `Invalid handle. Cannot load symbol cudnnCreate` | cuDNN DLL 环境问题, 在脚本 import torch 后加 `torch.backends.cudnn.enabled = False` (慢3-5倍但可用) |
| 训练时 `BrokenProcessPool` | 用 `--workers 1` (单进程 fbank 缓存) |
| ERes2NetV2 训练 OOM (6GB显存) | `--batch 32` |
| 模型下载慢 | ModelScope 首次下载较大, 可设置 `MODELSCOPE_CACHE` 到指定目录 |
| 结果与官网排名不符 | 确保用官方 CER 代码口径 (NFKC+lowercase+P*过滤+micro), 见 tools/verify_submission_official_cer.py |

## 六、提交文件说明

`results/submission_datasetA_dual.json` 是 datasetA 测试结果提交文件 (美的群通知新格式):

```json
{
  "result": {
    "results": [{"id": "id1", "content": "xxx", "label": "xxx", "cer": 0.0}, ...],
    "avg_cer": 0.379310,   // pos部分, 越小越好
    "avg_rr": 0.995781,    // neg部分, 越大越好
    "duration": 1426590    // 总推理时间, 单位ms
  }
}
```

- 所有 cer 均用官方代码重算 (与官方脚本逐条一致, 已用官方代码验证 0 差异)
- 提交时机: 8月中旬前提交 datasetA 结果 (网站排名参考); 8月中旬后提交模型+脚本 (等公司IT通知)

## 七、训练脚本 (如需重新微调)

```bash
# CAM++ 微调 (fold_0 五折交叉验证)
python tools/train_camplus_finetune.py --aug_root ../datasetA_aug_processed --fold 0 --epochs 10 --lr 1e-4 --batch 64

# ERes2NetV2 微调 (注意: 6GB显存用 batch 32 + workers 1)
python tools/train_eres2netv2_finetune.py --aug_root ../datasetA_aug_processed --fold 0 --epochs 10 --lr 1e-4 --batch 32 --workers 1

# 全量训练 (fold_full, 最终模型)
python tools/train_camplus_finetune.py --aug_root ../datasetA_aug_processed --fold full --epochs 10 --lr 1e-4 --batch 64
python tools/train_eres2netv2_finetune.py --aug_root ../datasetA_aug_processed --fold full --epochs 3 --lr 1e-4 --batch 32 --workers 1

# 增量训练 (新样本, 从已有权重继续)
python tools/train_camplus_finetune.py --aug_root <新数据> --fold full --epochs 10 --lr 1e-5 \
    --init_from runs/camplus_v7_full/camplus_finetuned_best.pt
```

## 八、版本历史

| 版本 | 说明 | 80分 |
|---|---|---|
| V2 | 无分离基线 | 54.04 |
| V4.1 | 自适应分离+声纹选轨 | 54.52 |
| V5 | CAM++微调 | 59.36 |
| V7 | CAM++v7微调 | 61.32 |
| **V8** | **双微调 fold_full 融合 (当前)** | **64.66** |
