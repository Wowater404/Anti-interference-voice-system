# 声纹模型重训说明（两台电脑分工）

> 用途：把流水线打包到另一台电脑，两台电脑分别训练 CAM++ 和 ERes2NetV2。
> 关键：本轮重训从**预训练模型从头训练**（不继承旧权重）；训练数据需先用新脚本清洗（kws 也清洗，与推理侧对齐）。

> ⚠️ **收到包后必读的两个前提**：
> 1. **环境**：这台电脑必须先配好 `zhinnegjiaju` 环境（Python + PyTorch CUDA + modelscope/funasr/speechbrain 等），否则清洗和训练都跑不起来，详见「三、环境要求」。
> 2. **顺序**：解压后**先跑清洗、再跑训练**，顺序不能反——否则会再次用回脏 kws 训练，本轮改动白做。

---

## 一、代码位置（打包源）

```
F:\龙虾\2026-07-18-13-57-00\voice_pipeline
```

数据目录（同一父目录下）：

| 目录 | 说明 | 大小 |
|------|------|------|
| `F:\龙虾\2026-07-18-13-57-00\datasetA_aug` | 增强数据（加噪/变声，未清洗） | 2.2G |
| `F:\龙虾\2026-07-18-13-57-00\datasetA_aug_processed` | 清洗后数据（**旧版，kws 未清洗，需重新生成**） | 2.2G |
| `C:\Users\善水\Desktop\datasetA\datasetA` | 原始测试集 A | 278M |

---

## 二、打包清单（给另一台电脑）

### 必须打包

1. **代码**（排除大目录，核心代码只有几 MB）：
   ```
   voice_pipeline/
   ├── pipeline.py  run_inference.py  config.py  requirements.txt
   ├── configs/  modules/  utils/  tools/
   └── （含本次改动的所有训练脚本）
   ```
   排除：`.git`、`results/`、`runs/`、`finetuned_models/`、`__pycache__/`、`*.pyc`

2. **预训练权重**（`voice_pipeline/pretrained/` 里的这几个，训练和数据处理都要用）：
   | 目录 | 用途 | 大小 |
   |------|------|------|
   | `modelscope_cache/` | CAM++/ERes2NetV2/ASR 预训练缓存 | 4.2G |
   | `spex_plus/` | SpEx+（数据处理时分离用） | 128M |
   | `sepformer16k/` | SepFormer16k（kws 盲分离用） | 108M |
   | `wespeaker_resnet34/` | ResNetSE（声纹 ensemble 用） | 26M |
   | `gtcrn/` | GTCRN（备选降噪） | 1.3M |

   > 不需要打包：`ecapa_tdnn/`、`sepformer/`（8kHz 已弃用）

3. **增强数据** `datasetA_aug`（2.2G）—— 清洗脚本的输入（另一台电脑从清洗开始，无需再增强）。

### 可选（如果另一台电脑能联网下载）

CAM++/ERes2NetV2 的预训练权重（ModelScope）、SepFormer16k（hf-mirror）、SpEx+（gdown）都能运行时自动下载，`modelscope_cache` 4.2G 可考虑不打包、让另一台电脑联网下（但国内网络建议打包带上）。

---

## 三、环境要求

参考本机 `zhinnegjiaju` 环境（`/f/work/Anaconda/envs/zhinnegjiaju`）：

- Python 3.12、PyTorch 2.x + torchaudio（CUDA 版）
- 依赖：`pip install -r requirements.txt`（含 modelscope、funasr、speechbrain、sherpa-onnx、noisereduce 等）
- Windows 需 PATH 含 conda 的 `Library/bin`（cuDNN DLL）

---

## 四、完整数据流

> 增强（①）已在本机完成，`datasetA_aug` 已打包进 tar。另一台电脑解压后**从 ② 清洗开始**，跳过 ① 增强。

```bash
cd voice_pipeline

# ① 增强（原始 datasetA → 增强数据，每样本 8 版本）
python tools/augment_dataset.py \
  --src "C:/Users/善水/Desktop/datasetA/datasetA" \
  --dst "F:/龙虾/2026-07-18-13-57-00/datasetA_aug" --workers 8

# ② 清洗（kws 降噪+自适应盲分离，cmd 降噪+分离）★本轮核心改动
python tools/prepare_processed_train_data.py \
  --data_root "F:/龙虾/2026-07-18-13-57-00/datasetA_aug" \
  --out_root "F:/龙虾/2026-07-18-13-57-00/datasetA_aug_processed" \
  --workers 4 --device auto

# ③ 划分 fold（按 orig_id 防泄漏，读 pos_aug_processed.jsonl）
python tools/make_folds.py \
  --aug_root "F:/龙虾/2026-07-18-13-57-00/datasetA_aug_processed" --n_folds 5
```

> ② 会同时生成 `kws_*_processed.wav` 和 `cmd_*_processed.wav`（本轮起 kws 也清洗了）。
> ③ 已改为自动读 `pos_aug_processed.jsonl`（无需手动复制）。

---

## 五、两台电脑分工训练

**电脑 A —— 训 CAM++**：
```bash
cd voice_pipeline
python tools/train_camplus_finetune.py \
  --aug_root "path/to/datasetA_aug_processed" \
  --fold full --epochs 10 --lr 1e-4 --batch 64 --workers 8
```
产出：`runs/fold_full/camplus_finetuned_best.pt`

**电脑 B —— 训 ERes2NetV2**：
```bash
cd voice_pipeline
python tools/train_eres2netv2_finetune.py \
  --aug_root "path/to/datasetA_aug_processed" \
  --fold full --epochs 10 --lr 1e-4 --batch 64 --workers 8
```
产出：`runs/fold_full/camplus_finetuned_best.pt`（文件名同，注意用不同 `--out_dir` 区分，或直接分机保存）

> 两台电脑都要有完整的 `datasetA_aug_processed`（含 `folds/` 目录）。

---

## 六、重训注意事项（重要）

1. **从预训练从头训，不继承旧权重**：CAM++ 脚本已强制 `init_from=None`（第 416 行），即使传了 `--init_from` 也被忽略；ERes2NetV2 本来就没有继承功能。旧的 `finetuned_models/`、`runs/` 权重**不要**用于本轮训练。
2. **训练数据必须用新的 `datasetA_aug_processed`**（kws 已清洗），不要用旧的（kws 未清洗的版本会导致训练/推理分布不匹配）。
3. **`--fold full` 是全量训练**（验证集并入训练，EER 监控失效，只产最终权重）；如需交叉验证用 `--fold 0`~`4`。
4. 训练完成后，把新权重放回 `finetuned_models/`（或 `runs/*_v7_full/`），更新 `configs/verify_dual_full.yaml` 里的 `voiceprint.cam_plus.finetuned_path` / `eres2netv2.finetuned_path`。
5. 两个模型都重训完成后，记得恢复 CAM++ 脚本的 `--init_from`（第 416 行改回 `init_from=args.init_from`），以便后续增量训练。

---

## 七、本次改动的文件清单

| 文件 | 改动 |
|------|------|
| `pipeline.py` | kws 自适应处理（降噪→唤醒词匹配→不够才盲分离）+ 接入两条推理路径 |
| `tools/prepare_processed_train_data.py` | kws 也走 `_process_kws`，与推理侧对齐 |
| `tools/train_camplus_finetune.py` | 强制从预训练重训（禁用 `--init_from` 继承） |
| `tools/make_folds.py` | 优先读 `pos_aug_processed.jsonl` |
| `configs/verify_dual_full.yaml` / `default.yaml` | 加 `kws_enable` / `kws_trigger_sim` |
| `run_inference.py` | 加 `--kws-text` 参数 |
